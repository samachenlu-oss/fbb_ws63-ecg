# fbb_ws63-ecg

This repository tracks only `applications/sample/wifi-iot/app` from the local
`ws63_ohos` workspace.

Use it as a code-sync layer on top of a separately prepared full OHOS/WS63
development environment.
